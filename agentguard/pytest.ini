[pytest]
testpaths    = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts      = -v --tb=short
markers =
    slow: slow tests (excluded from default run)
    integration: requires live daemon
    kernel: requires root + eBPF support
